name: Skills Lint Workflow

on:
  workflow_call:
    inputs:
      skills_directories:
        required: false
        type: string
        default: ""
        description: "Space-separated list of root directories containing skill sub-folders to validate (passed as --skills-directory flags). If empty, skills_lint falls back to its defaults (.claude/skills and .agents/skills)."
      skills_lint_version:
        required: false
        type: string
        default: ""
        description: "Version constraint for the skills_lint package (e.g. ^0.5.0). If empty, activates the latest version."
      dart_sdk:
        required: false
        type: string
        default: "stable"
      runs_on:
        required: false
        type: string
        default: "ubuntu-latest"
      working_directory:
        required: false
        type: string
        default: "."

jobs:
  build:
    defaults:
      run:
        working-directory: ${{inputs.working_directory}}

    runs-on: ${{inputs.runs_on}}

    steps:
      - name: 📚 Git Checkout
        uses: actions/checkout@v7

      - name: 🎯 Setup Dart
        uses: dart-lang/setup-dart@v1.8.1
        with:
          sdk: ${{inputs.dart_sdk}}

      - name: 📦 Activate skills_lint
        run: dart pub global activate skills_lint ${{inputs.skills_lint_version}}

      - name: 🔍 Lint Skills
        run: |
          FLAGS=""
          for dir in ${{inputs.skills_directories}}; do
            FLAGS="$FLAGS --skills-directory $dir"
          done
          dart pub global run skills_lint $FLAGS
